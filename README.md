# AI English Studio - 英语口语学习平台

专为油管、tiktok、x等平台的英语口语设计的学习网站，支持视频学习、语音评测、单词本等功能。

## 目录

- [项目简介](#项目简介)
- [技术栈](#技术栈)
- [环境要求](#环境要求)
- [快速开始](#快速开始)
- [Docker 部署](#docker-部署)
- [本地开发](#本地开发)
- [管理员账号](#管理员账号)
- [项目结构](#项目结构)

## 项目简介

AI English Studio 是一个英语口语学习平台，主要功能包括：

- 🎬 **视频学习** - 支持双语字幕的视频播放
- 🎤 **语音评测** - 专业级语音评测，提供发音评分和反馈
- 📚 **单词本** - 收藏生词，支持复习功能
- 📊 **学习统计** - 记录学习进度和时长
- 🎥 **本地视频学习** - 支持上传本地视频和字幕进行学习
- 🔐 **用户管理** - 完整的用户注册、登录系统
- 🛠️ **管理后台** - 视频管理、分类管理、授权码管理等

## 技术栈

### 前端
- **React 18** - 前端框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **shadcn/ui** - UI 组件库
- **React Router** - 路由管理
- **TanStack Query** - 数据请求

### 后端
- **Express.js** - Node.js Web 框架
- **SQLite** - 轻量级数据库
- **JWT** - 身份认证
- **TypeScript** - 类型安全

## 环境要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 或 **bun** >= 1.0.0
- **Docker** (推荐，用于生产部署)

## 快速开始

### 1. 克隆项目

```bash
git clone git@github.com:tkwa12358/ai-english-studio.git
cd ai-english-studio
```

### 2. 安装依赖

```bash
# 前端依赖
npm install

# 后端依赖
cd backend && npm install && cd ..
```

### 3. 配置环境变量

复制 `.env.example` 到 `.env` 并根据需要修改：

```bash
cp .env.example .env
```

### 4. 启动开发服务器

```bash
# 启动后端 (端口 3001)
cd backend && npm run dev

# 新终端启动前端 (端口 3000)
npm run dev
```

访问 http://localhost:3000 查看应用。

## Docker 部署

### 一键启动

```bash
# 构建并启动所有服务
docker-compose up -d --build
```

### 访问服务

- **前端**: http://localhost:3000
- **后端 API**: http://localhost:3000/api

### 停止服务

```bash
docker-compose down
```

详细部署说明请参考 [DEPLOYMENT.md](./DEPLOYMENT.md)

## 本地开发

### 后端开发

```bash
cd backend
npm install
npm run dev
```

后端服务运行在 http://localhost:3001

### 前端开发

```bash
npm install
npm run dev
```

前端服务运行在 http://localhost:3000

## 管理员账号

### 默认管理员

系统预设管理员账号：

- **邮箱**: `admin@163.com`
- **密码**: `admin@163.com`

### 初始化管理员

如果管理员账号不存在，可通过 API 初始化：

```bash
curl -X POST http://localhost:3000/api/admin/init
```

## 项目结构

```
├── public/                 # 静态资源
│   └── dictionaries/       # 词典文件
├── src/
│   ├── components/         # React 组件
│   │   ├── admin/          # 管理后台组件
│   │   └── ui/             # shadcn/ui 组件
│   ├── contexts/           # React Context
│   ├── hooks/              # 自定义 Hooks
│   ├── lib/                # 工具函数
│   │   └── api-client.ts   # API 客户端
│   ├── pages/              # 页面组件
│   │   └── admin/          # 管理后台页面
│   ├── App.tsx             # 应用入口
│   ├── index.css           # 全局样式
│   └── main.tsx            # 渲染入口
├── backend/
│   ├── src/
│   │   ├── app.ts          # Express 主入口
│   │   ├── config/         # 配置文件
│   │   ├── middleware/     # 中间件
│   │   ├── routes/         # API 路由
│   │   └── services/       # 业务服务
│   ├── package.json
│   └── tsconfig.json
├── data/                   # SQLite 数据库目录
├── docs/                   # 项目文档
├── docker/                 # Docker 配置
├── uploads/                # 用户上传文件
├── .env.example            # 环境变量示例
├── docker-compose.yml      # Docker 编排配置
├── Dockerfile              # Docker 构建文件
├── package.json            # 项目依赖
├── tailwind.config.ts      # Tailwind 配置
├── tsconfig.json           # TypeScript 配置
└── vite.config.ts          # Vite 配置
```

## API 文档

### 认证 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/auth/me` | GET | 获取当前用户 |

### 视频 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/videos` | GET | 视频列表 |
| `/api/videos/:id` | GET | 视频详情 |
| `/api/videos` | POST | 创建视频 (管理员) |
| `/api/videos/:id` | PUT | 更新视频 (管理员) |
| `/api/videos/:id` | DELETE | 删除视频 (管理员) |

### 学习 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/learning/progress` | GET | 获取学习进度 |
| `/api/learning/progress` | POST | 更新学习进度 |
| `/api/learning/statistics` | GET | 获取学习统计 |

### 单词 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/words/book` | GET | 获取单词本 |
| `/api/words/book` | POST | 添加单词 |
| `/api/words/book/:id` | DELETE | 删除单词 |
| `/api/words/lookup/:word` | GET | 查询单词 |

## 常见问题

### 1. 登录后显示空白页面

检查是否正确配置了 `.env` 环境变量。

### 2. 无法访问管理后台

确保用户已被分配了 `admin` 角色。

### 3. 后端 API 连接失败

检查后端服务是否正常运行在 3001 端口。

### 4. 视频上传失败

检查 `uploads` 目录是否有写入权限。

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
